// src/pages/StockInPage.js
import PageHeader from "../components/PageHeader";

function StockInPage() {
  return (
    <section>
      <PageHeader title="Stock In" subtitle="Add new stock to inventory." />
      <div className="placeholder-card">Stock In screen is scheduled later.</div>
    </section>
  );
}
export default StockInPage;
